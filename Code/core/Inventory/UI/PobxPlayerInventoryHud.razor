@using System
@using Sandbox
@using Sandbox.UI
@inherits PanelComponent
@namespace Conna.Inventory

@if (Player.InventoryComponent.Inventory is null)
	return;

<root>
	<PobxPlayerInventoryPanel @ref="InventoryPanelRef" Title="Inventory" Inventory="@Player.InventoryComponent.Inventory" />
</root>

@code {
	private PobxPlayer Player { get; set; }
	private PobxPlayerInventoryPanel InventoryPanelRef { get; set; }

	protected override void OnTreeFirstBuilt()
	{
		base.OnTreeFirstBuilt();
		// Log.Info(Inventory);


		StateHasChanged();
	}

	protected override void OnAwake()
	{
		base.OnAwake();

	}

	protected override void OnStart()
	{
		/*
		Player = Scene.Get<PobxPlayer>();
		Inventory = Player.InventoryComponent.Inventory;

		Assert.True(Player.IsValid());

		Assert.True(Inventory != null);
		*/
	}

	public void PlayerInitialized()
	{
		Player = PobxPlayer.FindLocalPlayer();

		Assert.True(Player.IsValid());
		
			// Assert.True(Inventory != null);
	}

	public void HandleInput()
	{
		
	}
}
